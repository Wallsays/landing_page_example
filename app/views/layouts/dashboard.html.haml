!!!
%html  
  = render :partial => "layouts/html_head"
  %body
    - flash.each do |key, value|
      %div{:class => flash_class(key), :id => "flasher"}
        = value
    = render :partial => "layouts/dash_navbar"
    = yield